<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="CarList.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:model="clr-namespace:CarList.Models"
    xmlns:viewmodel="clr-namespace:CarList.ViewModels"
    Title="{Binding Title}"
    x:DataType="viewmodel:CarListViewModel">

    <Grid
        ColumnDefinitions="*,*"
        ColumnSpacing="5"
        RowDefinitions="*, Auto">

        <CollectionView
            Grid.ColumnSpan="2"
            ItemsSource="{Binding Cars}"
            SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="model:Car">
                    <Frame HeightRequest="90">
                        <HorizontalStackLayout Padding="10" Spacing="5">
                            <Label Text="{Binding Make}" />
                            <Label Text="{Binding Model}" />
                        </HorizontalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button
            Grid.Row="1"
            Command="{Binding GetCarListCommand}"
            IsEnabled="{Binding IsLoading}"
            Text="Fetch Cars" />

    </Grid>

</ContentPage>
